# This file is sourced by Xcode to setup the environment for build scripts.
export NODE_BINARY=$(which node)
export PATH="$PATH:$(dirname $NODE_BINARY):/Users/sirawut/.cargo/bin:/opt/homebrew/bin:/usr/local/bin"
if [ -f "$HOME/.cargo/env" ]; then source "$HOME/.cargo/env"; fi
